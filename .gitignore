# Dependencies
node_modules

# Build directory
.next

# Optional local environment variables
.env.local
.env.*.local

# Optional lock files
# package-lock.json (if you use npm and want others to use exact versions, keep this *out* of .gitignore)
# yarn.lock (if you use yarn)

# Log files
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Optional editor directories
.vscode
.idea
